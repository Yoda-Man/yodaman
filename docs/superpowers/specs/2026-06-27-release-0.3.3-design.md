# YodaMan 0.3.3 Release Design

Update all product, manual, extension, and website version surfaces to 0.3.3. Build macOS ARM64, Windows x64, Linux x64, and VS Code artifacts with the existing release tools, copy them into `website/downloads`, and update the static website links to the exact generated filenames.

Completion requires the full Jest suite, Vite build, release smoke test, successful packaging commands, and a link audit proving each local download target exists.
